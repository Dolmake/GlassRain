using UnityEngine;
using System.Collections;

namespace GlassUtils  {

    /// <summary>
    /// Default Enum with the Speeds
    /// </summary>
    public enum WindshieldSpeed : int { NO = 0, SLOW = 1, NORMAL = 2, FAST = 3 }

    /// <summary>
    /// Public Enum to control the Direction
    /// </summary>
    public enum WindshieldDirection { FORWARD, REVERSE, STOPPED }
}
